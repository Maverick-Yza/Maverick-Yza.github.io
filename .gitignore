# ==============================================
# 1. 系统缓存文件（Windows/Mac 跨平台兼容）
# ==============================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# ==============================================
# 2. Hexo 核心临时文件/依赖（关键忽略项）
# ==============================================
node_modules/        # 依赖包目录（无需提交，本地安装即可）
public/              # 静态文件目录（hexo g 生成，部署时自动推送）
.deploy*/            # 部署临时目录（匹配 .deploy 和 .deploy_git）
.deploy_git/         # 部分 Hexo 版本单独生成的部署目录
db.json              # Hexo 数据文件（自动生成，无需备份）
*.log                # 日志文件（错误日志、运行日志等）
_multiconfig.yml     # 你的自定义多配置文件（无需提交）

# ==============================================
# 3. 编辑器/IDE 缓存配置（避免跨设备冲突）
# ==============================================
.idea/               # IntelliJ 系列（IDEA、WebStorm 等）
.vscode/             # VS Code 缓存/配置
*.swp                # Vim 交换文件
*.swo                # Vim 交换文件（临时）
*.suo                # Visual Studio 解决方案缓存
*.ntvs*              # Visual Studio 相关缓存
*.njsproj            # Visual Studio 项目文件
*.sln                # Visual Studio 解决方案文件
*.sw?                # 其他编辑器交换文件（通配符）

# ==============================================
# 4. 敏感文件/环境变量（提前规避泄露风险）
# ==============================================
.env                 # 环境变量文件（存储密钥、Token 等）
.env.*               # 多环境变量文件（如 .env.dev、.env.prod）
!.env.example        # 例外：若有示例配置文件（无敏感信息），可保留

# ==============================================
# 5. 自定义冗余文件（根据个人项目补充）
# ==============================================
# 若后续有其他无需提交的文件，可添加到这里
# 示例：xxx.txt、temp/ 等